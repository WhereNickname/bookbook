# Bookbook 다음 작업 메모

마지막 업데이트: 2026-09-04

## 지금 상태

- 라이브 주소: https://bookbook-two.vercel.app/
- GitHub: https://github.com/WhereNickname/bookbook
- `main` 브랜치에 push하면 Vercel이 자동 배포한다.
- 현재 책 데이터는 `app/book-data.ts`의 『이방인』 원문 발췌 77문장과 프롤로그 2문장이다.
- 앱은 **일반**과 **전자책** 두 읽기 모드를 제공한다.

## 현재 구현된 기능

- 일반 모드
  - 세로 스크롤/스와이프로 한 문장씩 이동
  - 중앙 강조 문장, 위·아래 작은 문장
  - 작은 문장을 누르면 해당 문장으로 바로 이동
  - 중앙 문장은 최대 3줄이 되도록 크기를 조절
  - 프롤로그 2문장은 숫자 대신 `프롤로그`로 표시
- 전자책 모드
  - 왼쪽 정렬의 줄글 읽기 화면
- 상단 탭
  - `일반 | 전자책` 버튼으로 모드 전환
  - 가로 스와이프로도 모드 전환
- 우측 상단 메뉴
  - 중앙 문장 / 숫자·프롤로그 / 작은 문장의 폰트를 각각 따로 선택 가능
  - Bookk, SUIT, Pretendard 선택 가능
- 하단 글래스모피즘 탭바는 시각적 샘플용으로 들어가 있다.

## 주요 파일

- `app/page.tsx`: 화면, 제스처, 읽기 모드, 폰트 메뉴
- `app/book-data.ts`: 프롤로그와 『이방인』 문장 데이터
- `app/globals.css`: 레이아웃, 서체, 애니메이션, 모바일 스타일
- `README.md`: Node 설치와 로컬 실행 방법

## 로컬에서 다시 실행하기

Node.js LTS를 설치한 뒤 프로젝트 폴더에서 실행한다.

```bash
npm ci
npm run dev
```

브라우저에서 터미널에 나온 주소(대개 `http://localhost:5173`)를 열면 된다.

## 다음에 이어서 볼 것

- 실제 사용자 피드백을 받은 뒤 일반 모드의 문장 간격·중앙 위치를 미세 조정한다.
- 책 선택 첫 화면과 복수 책 데이터 구조가 필요해지면 `book-data.ts`를 책 단위 데이터로 분리한다.
- 현재는 샘플링 프로토타입이므로 로그인, 저장 동기화, 실제 독서 진도 같은 완성형 기능은 아직 넣지 않았다.

## 변경 후 확인 순서

```bash
npm run build
git status
git add .
git commit -m "변경 내용"
git push github main
```

`main` 푸시 뒤 Vercel 배포가 완료되면 라이브 주소에서 모바일 터치와 스와이프를 확인한다.
